using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CardFoodInfo : MonoBehaviour {

    public void TriggerCardInfoEasy(int i)
    {
        switch (i)
        {
            default:
            case (0):
                //SceneManager.LoadScene("Level");
                Debug.Log("Start works");
                break;
        }
    }

    public void TriggerCardInfoMedium(int i)
    {
        switch (i)
        {
            default:
            case (0):
                //SceneManager.LoadScene("Level");
                Debug.Log("Start works");
                break;
        }
    }

    public void TriggerCardInfoHard(int i)
    {
        switch (i)
        {
            default:
            case (0):
                //SceneManager.LoadScene("Level");
                Debug.Log("Start works");
                break;
        }
    }
}
